numpy>=1.24
scipy>=1.10
opencv-python-headless>=4.8
Pillow>=10.0
pyyaml>=6.0
# pandas 3.x changes the truthiness of an empty DataFrame.apply() result,
# which breaks anomalib's Folder dataset builder (folder.py's mask-matching
# check raises ValueError: truth value of a Series is ambiguous). Untested
# against anomalib past this pin; bump only after re-running the detector
# harness tests.
pandas>=2.0,<3
matplotlib>=3.7
scikit-image>=0.21
scikit-learn>=1.3
tqdm>=4.65

# GPU / deep learning (installed on Kaggle; not needed for CPU-only modules)
torch>=2.0
torchvision>=0.15
# anomalib's PyPI package installs almost no deps by default - the actual model
# stack (lightning, kornia, freia, open-clip-torch, ...) is gated behind the
# `core` extra. Plain `anomalib==1.1.0` imports but every model class fails
# with ModuleNotFoundError the first time you touch it.
anomalib[core]==1.1.0
# anomalib[core] pins timm<=1.0.3; a bare `timm>=0.9` resolves to something
# newer and conflicts with it.
timm>=0.9,<=1.0.3
lpips>=0.1.4
# Restormer's arch file (third_party/restormer/.../restormer_arch.py) imports
# this directly; not a transitive dep of anything else pinned above.
einops>=0.7
gradio>=4.0
requests>=2.28
# anomalib's models/__init__.py eagerly imports an unrelated video model
# (AI-VAD) that uses a legacy pkg_resources-based CLIP loader. setuptools
# >=81 dropped pkg_resources, so importing ANY anomalib model - not just
# AI-VAD - fails with ModuleNotFoundError without this pin.
setuptools<81

# dev / test
pytest>=7.0

# See src/detect/_compat.py for four more anomalib-vs-current-environment
# fixes (numpy>=2 removed np.sctypes, a Matplotlib API rename, a private
# rich.Console attribute rename) that are shimmed in code rather than
# pinned here, because pinning numpy<2 could not be verified on this Python
# 3.14 sandbox (no prebuilt wheel) and may or may not be needed on yours.
